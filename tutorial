pip3 install virtualenv (once only)
cd ./SampleFolder
virtualenv .venv --python=python3
source .venv/bin/activate
emacs requirements.txt (add numpy, jupyter, ...)
mkdir .aux (auxiliary files which should also be ignored from git)
emacs .gitignore (add .venv, .aux, ...)
emacs README.md
python3 -m pip install --user -r requirements.txt (see https://askubuntu.com/questions/1025793/running-pip3-importerror-cannot-import-name-main)
git init
git add . (excludes files/folders in .gitignore)
git commit -m "first commit"
git remote add origin https://github.com/nehemoro/sample.git (create this repo first on github)
git remote set-url origin https://github.com/USERNAME/REPOSITORY.git (change remote)
git push -u origin master
